/// Google Sign-In configuration.
/// Remplis les IDs client depuis la Google Cloud Console.
class GoogleAuthConfig {
  /// Android client ID (from Google Cloud Console → Credentials → Android)
  static const String androidClientId = '';

  /// iOS / macOS client ID (from Google Cloud Console → Credentials → iOS)
  static const String iosClientId = '';

  /// Web client ID (from Google Cloud Console → Credentials → Web)
  static const String webClientId = '';
}
